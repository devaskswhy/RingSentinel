# Web framework
fastapi==0.141.1
uvicorn[standard]==0.52.4

# Database + migrations
sqlalchemy==2.0.52
alembic==1.19.1
psycopg[binary]==3.3.4

# Config
pydantic-settings==2.15.0

# Graph / detection (Phase 3)
networkx==3.6.1

# Payments - TEST MODE ONLY (Phase 2)
razorpay==2.0.1

# HTTP client for the seed's webhook delivery (also a razorpay dependency,
# pinned explicitly because scripts/seed_rings.py imports it directly)
requests==2.34.2

# LLM case files (Phase 4) - authenticated via Claude subscription OAuth, not an API key
claude-agent-sdk==0.2.144

# Unit tests (backend/tests). Pure: no database, no network, no model calls -
# the properties needing those are proven by scripts/verify_*.py against a live
# database instead.
pytest==8.4.2
